DATABASE_URL="file:./dev.db"
GEMINI_API_KEY=""
# OPENAI_API_KEY="" # Optional fallback if GEMINI_API_KEY not set
ADMIN_PASSWORD="admin"
JWT_SECRET="complex-secret-key-change-me-in-prod" # Secret for signing session JWTs (separate from admin password)
# SIMULATE_EMAIL_FAILURE="true" # Uncomment to test email failure handling
# WEBHOOK_URL="" # Optional: Outbound webhook for ticket events (approve/escalate)